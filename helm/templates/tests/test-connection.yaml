apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "liatrio.fullname" . }}-test-connection"
  labels:
    {{- include "liatrio.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "liatrio.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
