apiVersion: v1
kind: Service
metadata:
  name: {{ include "liatrio.fullname" . }}
  labels:
    {{- include "liatrio.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "liatrio.selectorLabels" . | nindent 4 }}
